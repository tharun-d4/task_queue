services:
  db:
    image: postgres:18
    ports:
      - 127.0.0.1:9000:5432
    volumes:
      - pg_data:/var/lib/postgresql
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: task_queue_db

volumes:
  pg_data:

